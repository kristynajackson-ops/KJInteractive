{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/next",
      "config": {
        "rootDirectory": "frontend"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/strategy-interactive/:path*",
      "destination": "/:path*"
    }
  ]
}
